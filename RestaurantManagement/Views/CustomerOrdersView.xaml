<UserControl x:Class="RestaurantManagement.Views.CustomerOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RestaurantManagement.Views"
             xmlns:viewmodels="clr-namespace:RestaurantManagement.ViewModels"
             xmlns:converters="clr-namespace:RestaurantManagement.Converters"
             d:DataContext="{d:DesignInstance Type=viewmodels:CustomerOrdersViewModel}"
             mc:Ignorable="d"
             Loaded="CustomerOrdersView_OnLoaded">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                BorderBrush="#DDDDDD" 
                BorderThickness="0,0,0,1" 
                Background="White" 
                Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="Comenzile mele" 
                           FontSize="24" 
                           FontWeight="SemiBold"/>
                
                <StackPanel Grid.Column="1" 
                            Orientation="Horizontal">
                    <CheckBox Content="Doar comenzi active" 
                              IsChecked="{Binding ShowActiveOnly}" 
                              VerticalAlignment="Center" 
                              Margin="0,0,15,0"/>
                    
                    <Button Content="Reîmprospătare" 
                            Command="{Binding LoadOrdersCommand}" 
                            Padding="15,8"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Row="1">
            <!-- Loading indicator -->
            <Grid Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel VerticalAlignment="Center" 
                            HorizontalAlignment="Center">
                    <TextBlock Text="Se încarcă comenzile..." 
                               FontSize="16" 
                               Margin="0,0,0,10"/>
                    <ProgressBar IsIndeterminate="True" 
                                 Width="200" 
                                 Height="10"/>
                </StackPanel>
            </Grid>
            
            <!-- No orders message -->
            <Grid Visibility="{Binding Orders.Count, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=0}">
                <TextBlock Text="Nu aveți comenzi plasate." 
                           FontSize="16" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"/>
            </Grid>
            
            <!-- Orders list -->
            <ScrollViewer Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                          VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Orders}" 
                              Margin="20">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#DDDDDD" 
                                    BorderThickness="1" 
                                    CornerRadius="5" 
                                    Margin="0,0,0,20" 
                                    Padding="15" 
                                    Background="White">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- Order Header -->
                                    <Grid Grid.Row="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <StackPanel>
                                            <TextBlock Text="{Binding OrderCode}" 
                                                       FontWeight="Bold" 
                                                       FontSize="16"/>
                                            <TextBlock Text="{Binding OrderDate, StringFormat='Data: {0:dd.MM.yyyy HH:mm}'}" 
                                                       Foreground="#666666"/>
                                        </StackPanel>
                                        
                                        <Border Grid.Column="1" 
                                                Background="#E3F2FD" 
                                                Padding="10,5" 
                                                CornerRadius="3">
                                            <TextBlock Text="{Binding FormattedStatus}" 
                                                       FontWeight="SemiBold" 
                                                       Foreground="#1976D2"/>
                                        </Border>
                                    </Grid>
                                    
                                    <!-- Order Items -->
                                    <ItemsControl Grid.Row="1" 
                                                  ItemsSource="{Binding Items}" 
                                                  Margin="0,15,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,0,0,5">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="100"/>
                                                    </Grid.ColumnDefinitions>
                                                    
                                                    <TextBlock Text="{Binding Name}"/>
                                                    <TextBlock Grid.Column="1" 
                                                               Text="{Binding Quantity, StringFormat='x{0}'}" 
                                                               Margin="0,0,15,0"/>
                                                    <TextBlock Grid.Column="2" 
                                                               Text="{Binding TotalPrice, StringFormat={}{0:N2} lei}" 
                                                               TextAlignment="Right"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    
                                    <!-- Order Footer -->
                                    <Grid Grid.Row="2" 
                                          Margin="0,15,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <StackPanel>
                                            <TextBlock Text="{Binding FormattedEstimatedDelivery, StringFormat='Livrare estimată: {0}'}" 
                                                       FontWeight="SemiBold"/>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="Total: "/>
                                                <TextBlock Text="{Binding TotalCost, StringFormat={}{0:N2} lei}" 
                                                           FontWeight="Bold"/>
                                            </StackPanel>
                                        </StackPanel>
                                        
                                        <Button Grid.Column="1" 
                                                Content="Anulează comanda" 
                                                Command="{Binding DataContext.CancelOrderCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                                CommandParameter="{Binding}" 
                                                Padding="10,5" 
                                                Background="#FFEBEE" 
                                                Foreground="#D32F2F" 
                                                BorderBrush="#FFCDD2"
                                                Visibility="{Binding Status, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=anulata|livrata}"/>
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl> 